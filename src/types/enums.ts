export enum BUCKET_NAMES {
  CONVERSATION_FILES = "conversation-files",
  CONVERSATION_VOICE = "conversation-voice",
  CONVERSATION_IMAGES = "conversation-images",
  CONVERSATION_VIDEOS = "conversation-video",
}
